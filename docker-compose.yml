version: '3'
services:
  web:
    container_name: web
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - default
    volumes:
      - .:/app